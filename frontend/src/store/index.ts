/**
 * Global client state (if/when needed beyond TanStack Query's server-state
 * cache) is organized here as one module per domain slice.
 *
 * Intentionally empty at the foundation stage — no app-level state exists
 * yet.
 */
export {};
