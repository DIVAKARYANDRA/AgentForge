/**
 * Generic, framework-agnostic helpers live here (formatting, validation,
 * etc.). Note: className merging lives in `@/lib/utils` (the shadcn/ui
 * convention) — keep that distinction as the codebase grows.
 *
 * Intentionally empty at the foundation stage.
 */
export {};
