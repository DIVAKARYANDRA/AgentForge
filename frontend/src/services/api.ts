/**
 * HTTP client setup lives here (Axios instance, interceptors, base URL,
 * auth headers) once the backend contract is finalized.
 *
 * Axios is installed and ready — intentionally left unconfigured at the
 * foundation stage per project scope (architecture only, no API
 * integration yet).
 */
export {};
